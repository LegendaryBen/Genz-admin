import React from 'react'

const Category = ({name}) => {
    return (
        <div className='category'>
            {
                name
            }
        </div>
    )
}

export default Category
